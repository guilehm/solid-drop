import styled from "styled-components"

export const Title = styled.h1`
  font-size: 4rem;
  font-weight: 300;
  line-height: 60px;
  padding-bottom: 20px;
  margin: 0;
`

export const List = styled.ul`
  display: flex;

  & button {
    margin-right: 5px;
  }
`
